def LongestCommonString(v: str, w: str):
    """
    LongestCommonString: Finds the alignment of two strings that will create the longest common string between the two
    strings.
    """
    ScoreMatrix = 